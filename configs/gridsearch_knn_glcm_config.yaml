model:
  name: "KNNClassifier"
  params:
    n_neighbors: [3, 5, 7, 9]
    weights: ["uniform", "distance"]
    metric: ["euclidean", "manhattan"]

feature_extractor:
  name: "GLCMFeatureExtractor"

dataset:
  data_dir: "Dataset/Data"
  image_size: [224, 224]
  color_mode: "grayscale"

output:
  dir: "results/gridsearch"
